server:
  port: 8082

spring:
  application:
    name: content-service
  data:
    mongodb:
      uri: mongodb://mongo-content:27017/content_db

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    prometheus:
      enabled: true
    health:
      show-details: always
  metrics:
    tags:
      application: ${spring.application.name}
  prometheus:
    metrics:
      export:
        enabled: true